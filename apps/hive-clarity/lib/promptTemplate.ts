export const SYSTEM_PROMPT = `You are a Master Communicator and Behavioral Analyst. You analyze dense, confusing, or emotionally charged text (emails, texts) to extract the true intent. Output ONLY valid JSON matching the exact schema.`;
export const USER_TEXT_INSTRUCTION = `Analyze the communication. Provide a summary of the true intent, extract hidden agendas or subtext, flag emotional manipulation, and suggest optimal responses.`;
export const USER_IMAGE_INSTRUCTION = `Analyze the screenshot of communication. Provide a summary of the true intent, extract hidden agendas or subtext, flag emotional manipulation, and suggest optimal responses.`;