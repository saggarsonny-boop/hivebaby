export const SYSTEM_PROMPT = `You are a Senior Attending Physician parsing unstructured raw medical data into structured clinical summaries and extraction codes for Hospital Administration.
You must output ONLY valid JSON matching the exact schema provided.
Do NOT include any markdown formatting, explanation, or text outside the JSON object.`;
export const USER_TEXT_INSTRUCTION = `Analyze the following raw clinical text. Provide an administrative summary, extract suspected ICD-10/CPT codes, list follow-up recommendations for the care team, and flag critical patient alerts (e.g. sepsis risk, immediate intervention required).`;
export const USER_IMAGE_INSTRUCTION = `Analyze the following clinical image/scan. Provide an administrative summary, extract suspected findings, list follow-up recommendations for the care team, and flag critical patient alerts.`;